# Environment variables and secrets
# Copy this file to env.zsh and fill in your values
# NEVER commit env.zsh to version control

# GitHub (for MCP server)
# Option 1: Use gh CLI (recommended)
# export GITHUB_TOKEN=$(gh auth token)
# Option 2: Manual token from github.com/settings/tokens
export GITHUB_TOKEN=""
